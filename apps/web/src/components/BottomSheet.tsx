/**
 * Restaurant detail panel for mobile: slides up from bottom. Name, last updated, menu list, "Add Update" CTA.
 */
export function BottomSheet() {
  return (
    <div className="tablet:hidden fixed bottom-0 left-0 right-0 bg-white rounded-t-xl shadow-lg max-h-[70vh] overflow-auto">
      <div className="p-4">Restaurant detail (bottom sheet) — placeholder</div>
    </div>
  );
}
