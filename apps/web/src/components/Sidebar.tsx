/**
 * Restaurant detail panel for desktop: map + sidebar (e.g. 320–400px). Same content as BottomSheet.
 */
export function Sidebar() {
  return (
    <aside className="hidden tablet:block w-80 bg-white border-r border-gray-200 overflow-auto shrink-0">
      <div className="p-4">Restaurant detail (sidebar) — placeholder</div>
    </aside>
  );
}
