/**
 * Editable list for /verify: Category, Item name, Variant, Price (₱), Delete; Add row; "Confirm and post to map".
 */
export function VerificationList() {
  return (
    <div className="space-y-2">
      <p className="text-sm text-gray-600">Editable menu rows — placeholder</p>
      <button type="button" className="min-h-[44px] px-4 rounded-lg bg-green-600 text-white">
        Confirm and post to map
      </button>
    </div>
  );
}
